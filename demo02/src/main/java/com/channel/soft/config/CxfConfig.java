package com.channel.soft.config;

import org.springframework.context.annotation.Configuration;
@Configuration
public class CxfConfig {

   /* @Autowired
     private WebServiceImpl webService;
    @Autowired
    private Bus bus; //必须有的

    //终端路径
    @Bean
    public Endpoint endpoint() {
        EndpointImpl endpoint=new EndpointImpl();
        endpoint.setBus();
       *//* endpoint = new EndpointImpl(bus, webService);
        // endpoint.getInInterceptors().add(new AuthInterceptor());//添加校验拦截器
        endpoint.publish("/testWebservice");*//*
        return endpoint;
        }*/

    }
